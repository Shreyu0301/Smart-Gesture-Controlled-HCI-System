# MongoDB Atlas Connection String
# Replace placeholders with your actual credentials from MongoDB Atlas

# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB.URI=mongodb+srv://<your-username>:<your-password>@<your-cluster>.mongodb.net/?retryWrites=true&w=majority

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the actual .env file to Git (it's protected by .gitignore)
# 2. Copy this file to .env and replace all <placeholder> values
# 3. Get your connection string from MongoDB Atlas dashboard
# 4. Keep your credentials private and secure
